apiVersion: v1
kind: Service
metadata:
  name: liyec
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "liyec"
    port: 8080
  selector:
    app: liyec
