apiVersion: v1
kind: Service
metadata:
  name: uenow
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uenow"
    port: 8080
  selector:
    app: uenow
