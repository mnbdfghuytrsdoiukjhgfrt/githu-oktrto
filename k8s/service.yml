apiVersion: v1
kind: Service
metadata:
  name: greact
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "greact"
    port: 8080
  selector:
    app: greact
