apiVersion: v1
kind: Service
metadata:
  name: dancing
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dancing"
    port: 8080
  selector:
    app: dancing
